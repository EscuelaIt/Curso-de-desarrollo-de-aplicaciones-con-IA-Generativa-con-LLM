services:
  api:
    build: ./api
    ports: ["8000:8000"]
    env_file: [.env]
    volumes:
      - ./data:/app/data
      - ./docs-piloto:/app/docs-piloto:ro
    environment:
      DATA_DIR: /app/data
      DOCS_SEED_DIR: /app/docs-piloto
